<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Registrations - {{ $event->title }}</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            font-size: 12px;
        }
        h1 {
            color: #1f2937;
            border-bottom: 3px solid #3b82f6;
            padding-bottom: 10px;
            margin-bottom: 20px;
        }
        h2 {
            color: #4b5563;
            font-size: 14px;
            margin-top: 30px;
            margin-bottom: 10px;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }
        th, td {
            border: 1px solid #d1d5db;
            padding: 8px;
            text-align: left;
        }
        th {
            background-color: #f3f4f6;
            font-weight: bold;
            color: #1f2937;
        }
        tr:nth-child(even) {
            background-color: #f9fafb;
        }
        .event-info {
            background-color: #eff6ff;
            padding: 15px;
            border-radius: 5px;
            margin-bottom: 20px;
        }
        .info-row {
            margin: 5px 0;
        }
        .footer {
            margin-top: 40px;
            padding-top: 20px;
            border-top: 1px solid #e5e7eb;
            text-align: center;
            color: #6b7280;
            font-size: 10px;
        }
    </style>
</head>
<body>
    <h1>Event Registrations Report</h1>
    
    <div class="event-info">
        <h2>Event Information</h2>
        <div class="info-row"><strong>Title:</strong> {{ $event->title }}</div>
        <div class="info-row"><strong>Date:</strong> {{ $event->start_date->format('F d, Y h:i A') }}</div>
        <div class="info-row"><strong>Location:</strong> {{ $event->location }}</div>
        <div class="info-row"><strong>Generated:</strong> {{ now()->format('F d, Y h:i A') }}</div>
    </div>
    
    <h2>Approved Registrations ({{ $registrations->count() }})</h2>
    
    <table>
        <thead>
            <tr>
                <th>#</th>
                <th>Name</th>
                <th>Email</th>
                <th>Registration Date</th>
                <th>Status</th>
            </tr>
        </thead>
        <tbody>
            @forelse($registrations as $index => $registration)
                <tr>
                    <td>{{ $index + 1 }}</td>
                    <td>{{ $registration->user->name }}</td>
                    <td>{{ $registration->user->email }}</td>
                    <td>{{ $registration->registration_date->format('M d, Y h:i A') }}</td>
                    <td>{{ ucfirst($registration->status) }}</td>
                </tr>
            @empty
                <tr>
                    <td colspan="5" style="text-align: center; color: #6b7280;">No registrations found.</td>
                </tr>
            @endforelse
        </tbody>
    </table>
    
    <div class="footer">
        <p>Generated by Event Management System on {{ now()->format('F d, Y') }}</p>
    </div>
</body>
</html>
